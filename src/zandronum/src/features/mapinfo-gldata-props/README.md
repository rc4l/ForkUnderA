# mapinfo-gldata-props

**Keyword(s):** brightfog, skyrotate, skyrotate2, lightadditivesurfaces, lightmode, notexturefill  
**Provenance:** uzdoom@65e7b6dfa  **Class:** PARSE-ONLY

GL fog/sky/light/tonemap state; renderer-only.

Recognised by ZandroX and consumed cleanly (no parse error); logged once via the
`ZXUnhandledMapKeys[]` manifest in `src/zandronum/src/g_mapinfo.cpp` (authoritative
keyword→SHA tracker). Not implemented here.
