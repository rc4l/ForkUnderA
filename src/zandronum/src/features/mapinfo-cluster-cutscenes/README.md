# mapinfo-cluster-cutscenes

Cluster `intro` / `outro` / `gameover` — UZDoom cluster-enter/exit/game-over cutscenes.
**Provenance:** uzdoom@cda6394a9 · **Class:** NOT-PORTABLE (needs the cutscene engine).
Recognised + consumed cleanly at the cluster fallback; logged once "not supported in this port".
Helper `ZX_ReportUnhandledCluster` in `src/zandronum/src/g_mapinfo.cpp`.
