// [rc4l] Master E2E map + pawn registration for features/quake-movement. See MASTERTEST.md.

// `playerclasses` REPLACES the list rather than adding to it, so DoomPlayer is not selectable at
// all here. That closes the harness trap this feature hit twice: a map change used to silently
// revert the pawn to DoomPlayer, and a "Quake" sample was really a Doom one. Now the fallback is
// MvDoomCtl -- a name dumpactor reports, so the substitution can never go unnoticed.
gameinfo
{
	playerclasses = "MvDoomCtl", "MvQBase", "MvCore", "MvCap", "MvCpm",
		"MvJump", "MvJumpSlow", "MvJumpInf", "MvSJDelay", "MvAbsSJ", "MvAbsSJOff", "MvWallJump", "MvWallJumpV2", "MvDoubleTap",
		"MvEdgeJump", "MvGroundSJ", "MvUser4", "MvElevator", "MvElevatorOff", "MvJumpXY",
		"MvSlide", "MvClimb", "MvWallRun", "MvTiers", "MvCrouch", "MvAll", "MvSound", "MvSoundQuiet"
}

map MVTEST "Quake Movement Master Test"
{
	Sky1 = "SKY1", 0
	Cluster = 1
	Music = ""
	Next = "MVTEST"
	SecretNext = "MVTEST"
	NoIntermission
	AllowRespawn
	AllowCrouch
	AllowJump
}

clusterdef 1
{
	hub
}
