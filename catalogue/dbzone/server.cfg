// Doom Barracks Zone: the original nine maps, cooperative. Against the game rather than each other.

cooperative true

// Read off a running DBZ server rather than assembled bit by bit here.
sv_weaponstay true
sv_falldamage true
sv_oldfalldamage true
sv_samelevel true
sv_noarmor true
sv_itemrespawn true
compat_limited_airmovement true
sv_sharekeys true
sv_unblockplayers true
sv_nodrop true
sv_allowfreelook true

// Ultra-Violence.
skill 3

// No rotation on purpose. The pack's own mapinfo chains map01 through map09 and then back to the
// start, so listing the maps here would be a second opinion about an order it already gives.
sv_randommaprotation 0
