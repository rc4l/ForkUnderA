// Two lives, whatever the experience is. Exec'd after the experience's own cfg, so it wins.
//
// [rc4l] Separate from the three-life remix beside it rather than sharing one with a number in it:
// how many lives is the whole content of these, so a remix that took an argument would be a remix
// that does nothing until somebody sets it.

sv_maxlives 2