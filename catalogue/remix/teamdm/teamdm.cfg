// Teams, whatever the experience is. Exec'd after the experience's own cfg, so it wins.
//
// Zandronum's game modes are mutually exclusive, so naming this one turns the deathmatch underneath
// it off. That is the whole file: the maps, the limits and the flags are the experience's business
// and were identical in the copy this replaces.

teamplay true