// [rc4l] Exec'd on top of the experience's own cfg. Nothing here but the mode: the ball is
// spawned by the engine at a deathmatch start, and terminator earns FRAGS, so the maps and
// the limits the experience already set are exactly the right ones.

terminator true
